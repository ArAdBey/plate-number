name: plate_number
description: " A Flutter package for capturing and displaying vehicle plate numbers."
version: 0.0.1
homepage: https://github.com/ArAdBey/plate-number

environment:
  sdk: '>=	3.3.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  bloc_concurrency: ^0.2.2
  bloc: ^8.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
